describe('Licenses | check | INTEGRATION', () => {
  // 1. productUuid is not valid && granteeIds is valid
  // 2. productUuid is empty && granteeIds is valid
  // 3. productUuid is valid && granteeIds is empty
  // 4. productUuid is not valid && granteeIds is not empty
  // 5. productUuid is valid && granteeIds is not empty
  // 6. API Key is not valid
});
